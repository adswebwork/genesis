import { OgImageDirective } from './og-image.directive';

describe('OgImageDirective', () => {
  it('should create an instance', () => {
    const directive = new OgImageDirective();
    expect(directive).toBeTruthy();
  });
});
